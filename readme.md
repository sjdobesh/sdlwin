# **sdlwin**

SDL2 live png window.  
intended to read png bytes from memory and display in window without saving or writing to file system

```
git clone https://github.com/sjdobesh/win.git
```

## dependencies
SDL2

`sudo apt install libsdl2-2.0-0 libsdl2-dev libsdl2-image-dev`
